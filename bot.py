/gigimage — full guided flow (pick category → pick style → send title → optional hex color) → generates 3 style variations and sends them back
/count — word/character counter
/shorten — URL shortener (via TinyURL API)
Regenerate button on the results
