generates the actual images using Pillow: gradient backgrounds, category badges, auto-fitting/wrapping title text, and style-specific decorations (minimal/bold/professional/creative). No external APIs or paid image services needed — it's all drawn locally, so image generation is free and instant.

Deployment files — requirements.txt, Procfile, railway.json, .gitignore, .env.example, and a full README.md walking through:

Getting your token from @BotFather
Testing locally
Pushing to GitHub
Deploying to Railway (just set BOT_TOKEN in Railway's Variables tab — it auto-detects the rest)
